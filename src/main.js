import { createApp } from "vue";
import App from "./App.vue";
import router from "./router";

createApp(App).use(router).mount("#app");

// Kalit so'zlar
// Routing, createWebHistory, router-view, router-link to="/about", router-link-exact-active,
// use(router), views folder, index.js da Test componentga faqat path berilgan router-link berilmagan,
// components folder, router folder, oddiy routing, dynamic routing
